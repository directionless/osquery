version: 0.2

phases:
  install:
    commands:
      - echo Nothing to do in the install phase...
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - mkdir build
      - cd build
      - cmake -DOSQUERY_IGNORE_CMAKE_MAX_VERSION_CHECK=TRUE -DOSQUERY_TOOLCHAIN_SYSROOT=/usr/local/osquery-toolchain ..
      - make -j `nproc`
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - target/messageUtil-1.0.jar
